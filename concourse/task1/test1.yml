---
platform: linux

image_resource:
  type: registry-image
  source:
    repository: golang
    tag: 1

inputs:
- name: git-test
  type: git
  source:
    uri: https://github.com/Apton-riyazS/Daily-updates.git
    branch: master
plan:
  - get: git-test
    run:
      path: Daily-updates/concourse/task1/test.sh

  - put: repo-main
    params:
      # specify the "repo-main" artifact as the location
      repository: repo-main

